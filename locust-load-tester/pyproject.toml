[project]
name = "locust-load-tester"
version = "0.1.0"
description = "Locust load tests for 5 e-commerce microservices"
requires-python = ">=3.10"
dependencies = [
    "locust>=2.29",
]

[tool.setuptools]
py-modules = ["locustfile"]

[project.optional-dependencies]
dev = [
    "ruff",
]
